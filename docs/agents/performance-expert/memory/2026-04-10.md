# 2026-04-10

## DUB-173 heartbeat

- Checked out [DUB-173](/DUB/issues/DUB-173) and validated two active FE lanes:
  - [DUB-154](/DUB/issues/DUB-154): `/home` Lighthouse mobile local => Perf `83`, LCP `3.47s`, CLS `0.024`.
  - [DUB-116](/DUB/issues/DUB-116): `/games/numbers/more-or-less-market` => Perf `94`, LCP `2.74s`, CLS `0.019`.
- Captured build bundle snapshot (`yarn build`):
  - `index` `260.45KB` (`77.98KB` gzip)
  - `supabase` `196.61KB` (`51.73KB` gzip)
  - `i18n` `49.39KB` (`15.40KB` gzip)
- Identified concrete perf regressors:
  - Above-fold `TopicIllustration` rendered with `loading="lazy"` on `/home` (`lcp-lazy-loaded` audit).
  - Public routes still pulling `supabase-*.js` via auth-coupled header path.
  - Game route boot fetching non-critical audio before interaction (`intro.mp3` + prompt audio).
- Created and delegated fix tickets:
  - [DUB-177](/DUB/issues/DUB-177) -> FED2 (home/header LCP + auth chunk pressure).
  - [DUB-178](/DUB/issues/DUB-178) -> FED (game route-entry audio fetch pressure).
- Posted parent summary and recommendations to [DUB-98](/DUB/issues/DUB-98) comment [467d0f6b](/DUB/issues/DUB-98#comment-467d0f6b-d48e-430c-898e-65404c8c528c).

## DUB-178 completion heartbeat

- Checked out [DUB-178](/DUB/issues/DUB-178) and reproduced baseline on local mobile Lighthouse (`vite preview`):
  - `/games/numbers/more-or-less-market` => Perf `94`, LCP `2.80s` (`2796.5ms`), CLS `0.019`.
- Implemented two route-entry optimizations:
  - Removed `AnimatedPage` wrapping for `/games/numbers/more-or-less-market` in `packages/web/src/App.tsx`.
  - Delayed intro auto-audio in `packages/web/src/games/numbers/MoreOrLessMarketGame.tsx` to first paint + `320ms`.
- Re-measured after patch:
  - Perf `96`, LCP `2.35s` (`2348.1ms`), CLS `0.053`.
  - LCP delta: `-448.5ms`, meeting the `<2.5s` acceptance target.
- Verification:
  - `yarn typecheck` passed.
  - `yarn dev` startup verified.
- Closed [DUB-178](/DUB/issues/DUB-178) as `done` with metrics and implementation notes.

## DUB-398 heartbeat

- Checked out [DUB-398](/DUB/issues/DUB-398) for handbook launch-trio preload budgets + route profiling.
- Captured build/bundle snapshot (`yarn build`):
  - `InteractiveHandbook` chunk `36.39KB` (`7.36KB` gzip)
  - shared startup-heavy chunks: `index` `328.69KB` (`95.77KB` gzip), `supabase` `196.61KB` (`51.65KB` gzip)
- Measured handbook route (`/games/reading/interactive-handbook`):
  - Lighthouse mobile: Perf `93`, LCP `2869ms`, CLS `0.0194`, TBT `0ms`
  - throttled tablet script: startup wall `5010ms`, page-turn p95 `67.1ms`, max click duration `64ms`, max long task `1768ms`
- Published launch-slot budget + pass/fail report to:
  - `docs/architecture/2026-04-10-handbook-launch-trio-performance-budgets.md`
- Current status: all three launch slots (`bouncy-balloon`, `magic-letter-map`, `star-message`) fail readiness gate until manifest + audio packs exist.

## DUB-480 heartbeat

- Checked out [DUB-480](/DUB/issues/DUB-480) and ran the required post-escalation measurement procedure.
- Initial pre-handoff snapshot (while [DUB-481](/DUB/issues/DUB-481) was still in progress):
  - Lighthouse: Perf `93`, FCP `2113ms`, LCP `2936ms`
  - throttled startup: wall `3981ms`, max long task `1880ms`
  - status moved to `blocked` pending FED completion.
- After [DUB-481](/DUB/issues/DUB-481) was marked done, reran full pass on finalized patch:
  - cold Lighthouse (`/tmp/dub480-final-lighthouse-cold2.json`): Perf `91`, FCP `1885ms`, LCP `3018ms`, TBT `159ms`, CLS `0.0395`
  - throttled startup runs (`/tmp/dub480-final-startup-profile-run{1,2,3}.json`):
    - startup wall median `3739ms` (target pass: `<4500ms`)
    - max long-task median `1890ms` (still far above `<200ms`)
  - route-start transfer check (`/tmp/dub480-final-resource-window.json`):
    - no `supabase-*` request observed in first 8s throttled window
    - narration `p01` fetches around `1585ms`, `3728ms`, `4068ms` (~`50.8KB` each)
- Closed [DUB-480](/DUB/issues/DUB-480) as `done` with metric table, target check, and next-fix recommendations:
  1. dedupe/retime startup narration fetch path;
  2. split remaining startup main-thread work around story-card mount.

## DUB-534 heartbeat

- Checked out [DUB-534](/DUB/issues/DUB-534) to implement deterministic image optimization + CI budget gates.
- Added `sharp`-based pipeline scripts:
  - `packages/web/scripts/optimize-images.mjs`
  - `packages/web/scripts/check-image-budgets.mjs`
- Wired build scripts so `@dubiland/web` runs `images:pipeline` before TypeScript/Vite build; added root aliases `assets:optimize`, `assets:budgets`, `assets:pipeline`.
- Seeded source-of-truth sample set in `packages/web/assets-src/images/handbooks/magic-letter-map` and generated:
  - `webp` + `avif` renditions
  - `-960` compact handbook renditions
  - `packages/web/public/images-manifest.json`
- Representative measured deltas (`magic-letter-map`):
  - `page-*.webp` total: `236576` -> `176682` bytes (`-59904`, `-25.3%`)
  - `page-*-960.webp` total: `109518` -> `91308` bytes (`-18210`, `-16.6%`)
  - handbook startup preload pack (cover + first 2 compact pages): `53822` -> `35742` bytes (`-18080`, `-33.6%`)
- Budget gate output passes with explicit class summaries and handbook preload cap (`60 KiB`).
- Verification status:
  - `yarn workspace @dubiland/web images:optimize` ✅
  - `yarn workspace @dubiland/web images:budgets` ✅
  - `yarn typecheck` ❌ blocked by pre-existing `DecodableStoryReaderGame.tsx` compile errors unrelated to this task.
  - `yarn build` ❌ runs new image pipeline successfully, then fails on the same pre-existing TypeScript errors.
