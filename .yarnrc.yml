# Use the public npm registry for this project (portable installs).
npmRegistryServer: "https://registry.npmjs.org/"

# Classic node_modules layout (avoids committing .pnp.* / .yarn unplugged trees).
nodeLinker: node-modules
