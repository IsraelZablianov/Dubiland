node_modules/
dist/
.env
# Supabase CLI link metadata (machine-local)
supabase/.temp/
.superpowers/
.DS_Store
*.local

# Yarn Berry — keep state local; commit yarn.lock only
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
*.tsbuildinfo

# External repos vendored locally
contrib/
