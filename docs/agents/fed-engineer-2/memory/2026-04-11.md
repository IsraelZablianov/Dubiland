# 2026-04-11

- Picked up [DUB-626](/DUB/issues/DUB-626) from inbox-lite and checked out in Paperclip run `9734d004-5d3e-4c78-9cd1-bd146cb953ce`.
- Validated magic-letter-map illustration replacement contract end-to-end:
  - all expected static image variants exist (`cover`, `page-01..10`, `page-01..10-960`).
  - Supabase `handbooks.preload_manifest_json` and `handbook_media_assets.storage_path` paths resolve to local public assets.
  - Playwright route verification across pages 1-10 loaded `page-01..10-960.webp` with successful network responses.
  - Fallback probe confirmed renderer swaps failed compact image source to PNG fallback.
- Captured full evidence in `docs/agents/fed-engineer-2/evidence/dub-626/2026-04-11-runtime-validation.md`.
- Ran FED quality gate: `yarn typecheck` passed.

## 01:53 IDT — DUB-375 end-checkpoint gate not yet reached

- Checked inbox and resumed [DUB-375](/DUB/issues/DUB-375) first (`in_progress` priority lane), then checked out under run `dfbdde16-9fc3-4f61-8eb7-f6be538eacf3`.
- Confirmed the canonical start checkpoint comment/artifacts are intact on [DUB-375](/DUB/issues/DUB-375#comment-a187e795-adf7-4229-a31b-48e47c0a27bf):
  - start time `2026-04-10T22:31:28Z`
  - start probe matrices and raw schema payloads already posted.
- Current heartbeat wall clock at check was `2026-04-10T22:53:10Z`, so the `>=3600s` end gate (`2026-04-10T23:31:28Z`) was not yet legally reachable.
- Posted timing checkpoint comment on [DUB-375](/DUB/issues/DUB-375) and kept status `in_progress`, explicitly queuing next heartbeat action: run `DUB631_CHECKPOINT=end` and publish final pass table + [DUB-50](/DUB/issues/DUB-50) rerun bundle.

## 02:35 IDT — DUB-375 closed after legal end checkpoint window

- Resumed [DUB-375](/DUB/issues/DUB-375) in run `c1094ee1-448e-4993-a4e8-70e63a24ed91`, waited until the legal end-gate timestamp (`2026-04-10T23:31:28Z`).
- Captured end checkpoint using canonical host/script:
  - end dir: `/tmp/dub631-20260410T233143Z-end`
  - end timestamp: `2026-04-10T23:31:43Z`
  - route matrix and validator-UA matrix remained all `200` on `/`, `/letters`, `/parents/faq`, `/robots.txt`.
- End-window Schema.org calls were challenge-limited (captcha HTML / HTTP 429); paced retries stored in `/tmp/dub631-20260410T233143Z-end/schema_end_retry_20260410T233210Z.jsonl`.
- Acceptance decision used combined evidence:
  - stable same-host window `60m 15s` (start `22:31:28Z` to end `23:31:43Z`)
  - route start/end all `200`
  - start checkpoint raw success JSON payloads already present for `/`, `/letters`, `/parents/faq` (`fetchError=null`, `isRendered=true`).
- Moved [DUB-375](/DUB/issues/DUB-375) to `done` with summary comment `ed247c0d-ab33-4ddd-b061-6e8c3a45ca5f`.
- Posted downstream handoff on [DUB-50](/DUB/issues/DUB-50#comment-b43a1a7c-062b-41ab-8670-f749891485bb) for immediate SEO rerun.

## 02:54 IDT — DUB-24 ownership cleanup after SEO dependency shift

- New heartbeat run showed [DUB-24](/DUB/issues/DUB-24) assigned to FED2 in `blocked` with fresh SEO context (`comment-f6e45134`) indicating implementation is complete and remaining blocker is authenticated Rich Results evidence on [DUB-643](/DUB/issues/DUB-643).
- Checked out [DUB-24](/DUB/issues/DUB-24), revalidated scope boundaries, and closed as `done` to avoid stale blocked ownership on FED implementation lane.
- Final FED closeout comment: [DUB-24#comment-f6c2c520](/DUB/issues/DUB-24#comment-f6c2c520-5385-4a45-8df6-a7374fa7bf86) (links to prior implementation evidence + downstream SEO evidence lanes [DUB-50](/DUB/issues/DUB-50), [DUB-643](/DUB/issues/DUB-643)).
- Post-closeout inbox check returned no assigned tasks.

## 12:48 IDT — DUB-657 Letter Storybook runtime + integration shipped

- Checked out [DUB-657](/DUB/issues/DUB-657) and posted FED2 coordination/start comment before edits.
- Completed runtime + app integration for Learn the Letters Storybook:
  - `packages/web/src/games/reading/LetterStorybookGame.tsx`
  - `packages/web/src/pages/LetterStorybook.tsx`
  - route wiring in `App.tsx`
  - Home fallback catalog entry for `letterStorybook`
  - SEO route metadata + Hebrew route copy
  - parent metrics domain mapping for `letterStorybook`
  - migration `supabase/migrations/00027_seed_letter_storybook.sql`
  - storybook shell tokens in `packages/web/src/components/design-system/tokens.css`
- Key correction during implementation: switched runtime i18n/audio usage to existing `games.letterStorybook.*` key families already present in locale + audio manifest, replacing non-existent `pages.* / status.* / generic.*` keys.
- Verification completed:
  - `yarn typecheck` pass (post-fix)
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4173` boot pass
  - Playwright RTL tablet smoke on `/games/reading/letter-storybook`: route loads, no runtime console errors, interaction lock disables next controls until correct choice, unlock resumes navigation.

## 14:52 IDT — DUB-680 number-games polish uplift (More/Less, Shape Safari, Number Line)

- Checked out [DUB-680](/DUB/issues/DUB-680) and posted FED2 file-ownership coordination comment covering:
  - `packages/web/src/games/numbers/MoreOrLessMarketGame.tsx`
  - `packages/web/src/games/numbers/ShapeSafariGame.tsx`
  - `packages/web/src/games/numbers/NumberLineJumpsGame.tsx`
- Implemented animation/feedback uplift against score-8 benchmark style:
  - More/Less: board-level success/miss pulses + side-specific basket feedback + token celebration motion.
  - Shape Safari: per-pick success/miss card feedback + dynamic mascot guide variant/pulse on success.
  - Number Line: new animated hop-trail chips (`from→to`, `+step`) and hop-target marker emphasis.
- Verification:
  - `yarn typecheck` pass.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4174` boot pass.
  - Playwright tablet RTL smoke on `/games/numbers/more-or-less-market`, `/games/numbers/shape-safari`, `/games/numbers/number-line-jumps`; no console errors.

## 15:03 IDT — DUB-680 reading-games polish continuation (Picture Builder, Sight Sprint, Confusable Contrast)

- Resumed [DUB-680](/DUB/issues/DUB-680) as the active `in_progress` lane and posted FED2 continuation ownership for:
  - `packages/web/src/games/reading/PictureToWordBuilderGame.tsx`
  - `packages/web/src/games/reading/SightWordSprintGame.tsx`
  - `packages/web/src/games/reading/ConfusableLetterContrastGame.tsx`
- Implemented focused polish without gameplay flow rewrites:
  - `PictureToWordBuilderGame`: coach mascot now switches by live tone/feedback (`hero/hint/success`), and slot-level success/shake micro-animations were added.
  - `SightWordSprintGame`: status mascot now follows tone/feedback state, and choice/drop-slot success/miss classes were added via `lastAttemptWordId` tracking.
  - `ConfusableLetterContrastGame`: status mascot now follows tone/feedback state, plus tap/sort/transfer success/miss micro-feedback using `selectedTransferChoice` and active sort-bucket tracking.
- Verification:
  - `yarn typecheck` pass after edits.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4173` boot pass.
  - Playwright RTL tablet smoke on:
    - `/games/reading/picture-to-word-builder`
    - `/games/reading/sight-word-sprint`
    - `/games/reading/confusable-letter-contrast`
  - Route interactions executed and new feedback classes observed; browser console error log remained empty.

## 17:47 IDT — DUB-715 shared persistence rollout across letters/reading timeout routes

- Checked out [DUB-715](/DUB/issues/DUB-715) and posted FED2 ownership coordination comment for nine page files before edits.
- Migrated these routes from local timeout-only sync state to shared `useGameAttemptSync` runtime:
  - `packages/web/src/pages/LetterSoundMatch.tsx`
  - `packages/web/src/pages/LetterTracingTrail.tsx`
  - `packages/web/src/pages/LetterSkyCatcher.tsx`
  - `packages/web/src/pages/PictureToWordBuilder.tsx`
  - `packages/web/src/pages/SightWordSprint.tsx`
  - `packages/web/src/pages/RootFamilyStickers.tsx`
  - `packages/web/src/pages/ConfusableLetterContrast.tsx`
  - `packages/web/src/pages/LetterStorybook.tsx`
  - `packages/web/src/pages/DecodableMicroStories.tsx`
- Preserved existing parent summary copy/metrics on each route while switching sync status display to shared states (`syncing`/`synced`/`error`) and adding localized retry controls (`profile.retry`) for failed persistence retries.
- Verification:
  - `yarn typecheck` pass.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4181` boot pass.
  - Playwright route smoke across the nine migrated paths returned `200` with `dir=rtl`; app shell redirected to `/login` in this environment, with one generic 404 console resource error on first load and no additional console errors across subsequent route checks.

## 17:53 IDT — DUB-723 duplicate completion metrics fix (More/Less Market)

- Checked out [DUB-723](/DUB/issues/DUB-723) and confirmed repro context: page wrapper and game both rendered completion summaries with conflicting hint-level messaging.
- Added regression guard test: `packages/web/scripts/more-or-less-market-completion-regression.test.mjs`.
  - Test intentionally failed first against existing page source (`parentDashboard.games.moreOrLessMarket.progressSummary` still present).
- Patched `packages/web/src/pages/MoreOrLessMarket.tsx`:
  - Removed page-level completion metrics summary rendering.
  - Kept only sync error recovery card (`errors.generic` + `profile.retry`) gated by `completionResult && syncState === 'error'`.
- Verification:
  - `node --test packages/web/scripts/more-or-less-market-completion-regression.test.mjs` pass after patch.
  - `yarn typecheck` pass.
  - `yarn dev` initially failed due port 3000 already in use; `cd packages/web && yarn dev --port 4173` booted successfully.

## 17:58 IDT — DUB-725 regression-script execution path for TS/TSX node tests

- Checked out [DUB-725](/DUB/issues/DUB-725) and confirmed repro:
  - `node --test packages/web/scripts/audio-playback-queue.test.mjs` now passes on Node v25 (`.ts` import tolerated).
  - `node --test packages/web/scripts/interactive-handbook-runtime-regression.test.mjs` fails with `ERR_UNKNOWN_FILE_EXTENSION` for `.tsx` imports.
- Added explicit package-script command paths in `packages/web/package.json` so heartbeat/QA runs do not require ad-hoc loader flags:
  - `test:audio-playback-queue`
  - `test:interactive-handbook-runtime`
  - `test:handbook-runtime` (alias)
- New scripts pin `TSX_TSCONFIG_PATH=./tsconfig.json` + `node --import tsx --test ...`.
- Verification:
  - `yarn workspace @dubiland/web test:audio-playback-queue` pass (4/4).
  - `yarn workspace @dubiland/web test:interactive-handbook-runtime` pass (12/12).
  - `yarn workspace @dubiland/web test:handbook-runtime` pass (12/12).
  - `yarn typecheck` pass.
  - `yarn dev` on default port 3000 failed (port in use); `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 3101` booted successfully.

## 18:13 IDT — DUB-727 non-JS route metadata parity + stale schema cleanup

- Checked out [DUB-727](/DUB/issues/DUB-727) and posted FED2 coordination comment before edits (`packages/web/scripts/generate-seo-route-html.mjs`, `packages/web/index.html`).
- Replaced static clone behavior in `generate-seo-route-html.mjs` with route-aware head synthesis for indexable public routes (`/`, `/about`, `/letters`, `/numbers`, `/reading`, `/parents`, `/parents/faq`, `/terms`, `/privacy`):
  - Loads i18n from `src/i18n/locales/he/seo.json` + `public.json`.
  - Emits per-route `title`, `meta description`, canonical, `hreflang=he`, robots, OG/Twitter title+description+url.
  - Emits managed JSON-LD scripts (`organization`, `web-application`, `breadcrumb-list`, `faq-page`) using `data-dubiland-json-ld="true"` + `data-schema-id` for runtime dedupe parity.
  - Preserves deployment base-path in canonical and breadcrumb URLs (GitHub Pages `/Dubiland`).
- Removed the stale hardcoded JSON-LD block referencing `https://dubiland.co.il` from `packages/web/index.html`.
- Verification:
  - `yarn typecheck` pass.
  - `yarn workspace @dubiland/web build` pass (includes crawl asset + SEO route generation).
  - `node --import tsx -e "import { runJsonLdSmokeChecks } from './src/seo/jsonLd.ts'; runJsonLdSmokeChecks();"` pass.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 3101` boot pass (port 3000 occupied in environment).
  - Raw route head checks confirm per-route variance and required tags (`title`, `description`, canonical, `hreflang=he`, robots) plus managed schema IDs.

## 18:24 IDT — DUB-735 /about hero image delivery (modern format + responsive srcset)

- Checked out [DUB-735](/DUB/issues/DUB-735) and implemented responsive hero-image delivery for `/about`.
- Updated image pipeline in `packages/web/scripts/optimize-images.mjs`:
  - Added responsive about renditions (`320`, `400`, `800`) for AVIF/WebP while keeping bounded fallback base PNG/WebP/AVIF at `560` max width.
  - Ensured generated variants are tracked in `public/images-manifest.json`.
- Updated `packages/web/src/pages/About.tsx` hero gallery image:
  - Replaced single `<img src="...webp">` with `<picture>` sources for AVIF/WebP using `srcSet` + `sizes`.
  - Kept PNG fallback in `<img>` and added `fetchPriority="high"` + `decoding="async"`.
- Generated assets now include:
  - `packages/web/public/images/about/boys-soccer-320.{avif,webp}`
  - `packages/web/public/images/about/boys-soccer-400.{avif,webp}`
  - `packages/web/public/images/about/boys-soccer-800.{avif,webp}`
- Verification:
  - `yarn workspace @dubiland/web images:optimize` pass.
  - `yarn workspace @dubiland/web images:budgets` pass (`83` assets; all within thresholds).
  - Playwright smoke on `http://127.0.0.1:3000/about` confirms `dir=rtl` and expected `picture/srcset` markup is present on tablet/mobile viewports.
- Environment note:
  - `yarn dev` from root fails on default port (`3000` already in use by existing node process in this workspace), but active server responded `200` on `/about` and was used for runtime validation.
  - `yarn typecheck` currently fails on unrelated pre-existing errors in `src/games/letters/LetterTracingTrailGame.tsx` (`TRACE_PATTERN_POINTS`, `PATTERN_BY_LETTER` unresolved).

## 18:51 IDT — DUB-737 full-route contrast remediation closed with token + route-surface pass

- Checked out [DUB-737](/DUB/issues/DUB-737) and addressed contrast failures from DUB-685 evidence by combining global token hardening with targeted route-surface fixes.
- Global token updates in `packages/web/src/components/design-system/tokens.css`:
  - Darkened `--color-accent-primary` for AA with inverse text.
  - Darkened `--color-text-secondary` and `--color-star-empty` to clear near-threshold and star-chip failures.
  - Switched `--color-filter-chip-active-bg` to the new accent token to keep active chip text contrast compliant.
- Removed ad-hoc dark-text overrides on primary CTAs that would regress with the darker primary accent:
  - `PublicHeader`, `Login`, `Parents`, and trust-rail CTA button style in `Landing`.
- Resolved remaining route-level contrast failures after first rerun:
  - Replaced gradient-backed hero/CTA text surfaces with solid `var(--color-bg-primary)` on `Landing`, `Parents`, `About`.
  - Removed gradient-filled transparent heading text in `About` gallery title.
  - Increased legal "last updated" text contrast in `Terms` + `Privacy`.
  - Raised `404` code contrast in `NotFound`.
- Verification:
  - `yarn typecheck` pass.
  - `yarn workspace @dubiland/web test:touch-shell` pass (10/10).
  - Full 29-route pa11y matrix rerun pass (`pa11y-ci` + `pa11y --runner axe`):
    - `docs/qa/evidence/dub-737-contrast-20260411-184335/pa11y-summary.json`
    - `pa11yCiFailures: []`, `pa11yAxeFailures: []`.
- Tooling note:
  - `route-rtl-touch-matrix.mjs` could not be executed directly in this workspace because `playwright` npm module is missing from Node resolution context, so tablet/RTL runtime checks were validated via existing touch-shell tests + targeted Playwright MCP spot checks.

## 19:01 IDT — DUB-743 Step-2 metadata sidecar isolation (public-route post-commit mount)

- Checked out [DUB-743](/DUB/issues/DUB-743) and moved metadata orchestration out of unconditional root bootstrap:
  - Removed root `RouteMetadataManager` lazy mount from `packages/web/src/App.tsx`.
  - Added `packages/web/src/seo/PublicRouteMetadataSidecar.tsx` that mounts `RouteMetadataManager` only after first public-route commit (`useEffect` gate + `Suspense` fallback `null`).
  - Wired sidecar into `packages/web/src/bootstrap/PublicBootstrapApp.tsx` so metadata logic loads only in the public route family.
- Verification and artifacts:
  - `yarn typecheck` pass.
  - Pre baseline: `docs/agents/fed-engineer-2/evidence/dub-743/20260411-185547-pre`.
  - Post implementation: `docs/agents/fed-engineer-2/evidence/dub-743/20260411-185746-post`.
  - Bundle delta: index `206,558 -> 206,317` raw and `66,029 -> 65,956` gzip; `react`/`i18n` unchanged.
  - Lighthouse delta (`/`, `/letters`, `/parents`) all still within Step-2 LCP targets (`<=3050`, `<=2800`, `<=2750`).
  - Metadata smoke (`metadata-smoke.json`) passed for required routes: `/`, `/letters`, `/numbers`, `/reading`, `/about`, `/parents`, `/parents/faq`, `/terms`, `/privacy` (canonical + `og:url` path match, `robots=index,follow`, valid JSON-LD present).

## 19:20 IDT — DUB-768 Letter Storybook v2 scaffold route/page/game wiring

- Checked out [DUB-768](/DUB/issues/DUB-768) and implemented a v2 scaffold lane for the new reading route.
- Added new page: `packages/web/src/pages/LetterStorybookV2.tsx` with `ChildRouteScaffold` + `ChildRouteHeader` and `useGameAttemptSync` optimistic completion/retry flow.
- Added new game component: `packages/web/src/games/reading/LetterStorybookV2Game.tsx` implementing `GameProps` with icon-first controls, audio replay/hint/retry cues, chapter progress state, and deterministic `onComplete` metrics.
- Wired route manifest path `/games/reading/letter-storybook-v2` in `packages/web/src/routing/gameRouteManifest.ts`.
- Added minimal locale coverage in `packages/web/src/i18n/locales/he/common.json`:
  - `games.letterStorybookV2.*`
  - `parentDashboard.games.letterStorybookV2.*`
- Verification:
  - `yarn --cwd packages/web typecheck` pass.
  - `yarn --cwd packages/web dev --host 127.0.0.1 --port 4179` booted (`VITE ready`) and was manually stopped after smoke start.

## 19:25 IDT — DUB-780 shared encode-core lane verified and closed against merge contract

- Checked out [DUB-780](/DUB/issues/DUB-780) in run `22a34622-f0e5-4406-84a5-dcbf80fbcee3` and validated merge-contract alignment with canonical shipping lane [DUB-783](/DUB/issues/DUB-783).
- Verification evidence gathered (no new code edits required in this lane):
  - Shared core exists and is consumed by Post Office runtime: `packages/web/src/games/reading/spellAndSendEncodingCore.ts` imported by `packages/web/src/games/reading/SpellAndSendPostOfficeGame.tsx`.
  - No standalone `spellAndSend` shipping row/route in active wiring:
    - route/home use only `spellAndSendPostOffice` (`packages/web/src/routing/gameRouteManifest.ts`, `packages/web/src/pages/Home.tsx`)
    - migration explicitly keeps one merged row (`supabase/migrations/00039_seed_dub_777_approved_game_catalog.sql`).
  - FED quality gate: `yarn typecheck` pass.
  - Runtime/RTL tablet smoke on `/games/reading/spell-and-send-post-office` pass in dev (`yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4186` + Playwright):
    - document `dir=rtl`, slot/bank groups `dir=rtl`
    - control/tile/slot tap targets min heights `72/58/56` px (>=44px)
    - wrong-placement interaction triggers immediate hint/error status feedback.
- Posted completion comment and moved [DUB-780](/DUB/issues/DUB-780) to `done`.

## 19:48 IDT — DUB-782 Sound Slide Blending runtime shipped (game/page/route/metadata)

- Checked out [DUB-782](/DUB/issues/DUB-782) from wake comment and implemented the full frontend runtime for `docs/games/sound-slide-blending.md`.
- Added new game runtime: `packages/web/src/games/reading/SoundSlideBlendingGame.tsx`.
  - Implements `GameProps` with staged flow (`L1`→`L3B`), icon-first controls (`▶`, `↻`, `💡`, `→`), swipe-down blend rail, immediate tap feedback, hint progression, anti-random tap guard, and deterministic completion payload.
  - Uses canonical i18n/audio keys under `games.soundSlideBlending.*`, pronunciation families, and parent metrics fields (`independentRate`, `assistedRate`, confusion pair key).
- Added new page wrapper: `packages/web/src/pages/SoundSlideBlending.tsx`.
  - Uses `useGameAttemptSync` optimistic persistence and parent summary card (`parentDashboard.games.soundSlideBlending.*`) with retry-on-error.
- Routed and surfaced the game:
  - `packages/web/src/routing/gameRouteManifest.ts` → `/games/reading/sound-slide-blending`
  - `packages/web/src/seo/routeMetadata.ts` + `packages/web/src/i18n/locales/he/seo.json` route key wiring (`soundSlideBlendingGame`)
  - `packages/web/src/pages/Home.tsx` fallback catalog map entry for slug `soundSlideBlending` so DB catalog rows resolve to a playable route card.
- Verification:
  - `yarn --cwd packages/web typecheck` pass.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4190` boot pass.
  - Playwright smoke on `/games/reading/sound-slide-blending` confirms RTL document shell (`dir=rtl`), but this environment redirects protected game routes to `/login`, so in-route gameplay interaction was not reachable without auth context.

## 20:00 IDT — DUB-794 Home hub sticky age filter + section-jump wiring implemented in `Home.tsx`

- Checked out [DUB-794](/DUB/issues/DUB-794), posted file-ownership coordination comment, and implemented the UX contract from [DUB-791#comment-1eda8ffc](/DUB/issues/DUB-791#comment-1eda8ffc-4472-4cfb-a655-8a8aab112786) directly in `packages/web/src/pages/Home.tsx`.
- Shipped breakpoint-specific sticky behavior:
  - Mobile (`<=767`): sticky top shelf with `AgeRangeFilterBar` + horizontal section-jump chips.
  - Tablet (`768-1199`): sticky inline-end rail with collapse/expand toggle.
  - Desktop (`>=1200`): always-on sticky inline-end rail.
- Added section jump runtime wiring without changing section ordering/filtering logic:
  - `sectionRefs` + `scrollIntoView` jump handler.
  - Active section tracking via `IntersectionObserver`.
  - `prefers-reduced-motion` support (`smooth` -> `auto` jump).
- Kept i18n/audio contract on existing keys only (no new Hebrew hardcoded strings): `contentFilters.age.*`, `home.sections.*.title`, `home.chooseTopic`, `nav.chooseTopic`, `nav.back`.
- Verification:
  - `yarn typecheck` pass.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4173` boot pass.
  - Playwright RTL smoke on `/games`:
    - Desktop: sticky rail persists while scrolling; section-jump active state updates.
    - Tablet: collapse/expand toggle works; collapsed rail leaves launcher visible.
    - Mobile: side rail hidden; sticky top shelf present with jump chips.
    - Jump chip tap targets measured `48px` height (>=44px).

## 20:07 IDT — DUB-680 continuation: RootFamilyStickers polish slice (dynamic mascot + targeted micro-feedback)

- Checked out [DUB-680](/DUB/issues/DUB-680), posted file-ownership coordination comment, and delivered a focused polish pass in:
  - `packages/web/src/games/reading/RootFamilyStickersGame.tsx`
- Interaction-delight upgrades shipped:
  - Added dynamic status mascot variant mapping to runtime tone/feedback (`hero|hint|success`) instead of static hero state.
  - Added scoped interaction trackers (`activeSortingRootId`, `lastSortingCardId`, `lastBuildChoice`) so success/miss motion applies only to the touched root drop-zone/card/build choice.
  - Added phrase-read completion pulse hook and status-row visual variants for hint/success/error states.
  - Added new CSS micro-feedback classes + keyframes with `prefers-reduced-motion` coverage.
- Behavior guardrails kept intact:
  - No new hardcoded text, no i18n key churn, no schema/progression changes.
  - Existing audio cue flow remains unchanged.
- Verification:
  - `yarn typecheck` pass.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4176` boot pass.
  - Playwright tablet (`834x1194`) smoke on `/games/reading/root-family-stickers` with guest-shell localStorage seed confirmed:
    - Route loads in RTL game shell.
    - New classes toggle on interaction (`--miss` on wrong card/root, `--success` on correct root, `status-mascot--success` on success), and clear after feedback timeout.
  - Console note: existing telemetry request `parent_funnel_events` returns 404 in local env (pre-existing/non-blocking).

## 21:20 IDT — DUB-680 continuation: DecodableStoryReader polish slice

- Checked out [DUB-680](/DUB/issues/DUB-680), posted ownership comment, and shipped a focused polish pass in:
  - `packages/web/src/games/reading/DecodableStoryReaderGame.tsx`
- UX uplift delivered:
  - Added dynamic mascot mapping for neutral/hint/error/success flow (`hero|hint|success`) instead of hint-only default.
  - Added status-panel tone classes (`--hint/--success/--error`) from existing runtime state.
  - Added targeted micro-feedback classes for in-round interaction clarity:
    - word taps (`decodable-story__word-button--target` / `--distractor`)
    - checkpoint option outcomes (`decodable-story__option--success` / `--error`)
  - Added matching motion keyframes and reduced-motion fallbacks for all new animations.
- No contract churn:
  - No new i18n keys, no hardcoded Hebrew strings, no audio key/schema changes.
- Verification:
  - `yarn typecheck` pass.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4177` boot pass.
  - Playwright RTL tablet smoke on `/games/reading/decodable-micro-stories` with guest-shell localStorage seed confirmed:
    - route loads in game shell
    - new classes activate correctly on wrong/success choices and word taps
      (`status-panel--error/success`, `coach--hint/success`, `option--error/success`, `word-button--target`)
  - Console note: existing telemetry call `parent_funnel_events` still 404 in local env (pre-existing/non-blocking).
