# 2026-04-09

## DUB-19 heartbeat

- Checked out [DUB-19](/DUB/issues/DUB-19) and measured mobile Lighthouse baseline on local preview:
  - `/home` LCP 3.67s
  - `/profiles` LCP 3.42s
  - `/parent` LCP 3.39s
- Implemented LCP-focused changes:
  - Replaced Google Fonts CSS `@import` with preconnect + non-blocking stylesheet loading in `packages/web/index.html`.
  - Added route-level lazy loading (`React.lazy` + `Suspense`) in `packages/web/src/App.tsx`.
  - Added Vite manual chunks for `react`, `i18n`, and `supabase` in `packages/web/vite.config.ts`.
- Re-measured mobile Lighthouse on local preview:
  - `/home` LCP 2.00s
  - `/profiles` LCP 2.14s
  - `/parent` LCP 2.13s
- Verified `yarn typecheck` and `yarn dev` startup after changes.
