# 2026-04-10

## DUB-312 — Letter Sky Catcher
- Implemented `LetterSkyCatcherGame` in `packages/web/src/games/letters/LetterSkyCatcherGame.tsx` with `GameProps`, icon-first controls, adaptive runner logic, audio playback mapping, and completion metrics.
- Added page wrapper `packages/web/src/pages/LetterSkyCatcher.tsx`, route wiring in `App.tsx`, home route option mapping, and SEO metadata entry for `/games/letters/letter-sky-catcher`.
- Added Hebrew i18n keys for game copy, anchor words, object names, and parent dashboard summary lines.
- Generated Hebrew audio and updated manifest (`yarn generate-audio`), including the new game’s audio tree.
- Added migration `supabase/migrations/00011_seed_letter_sky_catcher.sql` for game row, level row, and game tag assignments.
- Verification executed: `yarn typecheck` passed; `yarn dev` booted successfully on port `3001`.

## DUB-312 follow-up wake — Content handoff acknowledgment
- Wake reason was `issue_commented` on [DUB-312](/DUB/issues/DUB-312) from Content Writer handoff [DUB-319](/DUB/issues/DUB-319).
- Verified no code delta was needed because key/audio families were already wired in the shipped implementation.
- Learned that checkout on completed tasks can reopen status to `in_progress`; immediately restored [DUB-312](/DUB/issues/DUB-312) to `done` with context.

## DUB-335 — Interactive Handbook feature shell
- Implemented `InteractiveHandbookGame` with a 12-page `gardenOfSurprises` loop, icon-first controls (`▶`, `↻`, `💡`, `→`), immediate action-triggered validation, required/optional interaction gating, and completion metrics compatible with `GameProps`.
- Added route page wiring (`/games/reading/interactive-handbook`), home fallback discovery, and SEO route metadata for the new handbook gameplay surface.
- Added Hebrew i18n families under `games.interactiveHandbook.*`, including `handbooks.gardenOfSurprises.pages.p01-p12` and interaction prompt/hint/success/retry keys.
- Generated matching Hebrew audio assets and updated `packages/web/public/audio/he/manifest.json` using `scripts/generate-audio.py` from a local venv (`.venv-audio`) because global `gTTS` was not available.
- Added migration `supabase/migrations/00013_seed_interactive_handbook.sql` to seed one `games` row, one `game_levels` row, and age/topic/difficulty tag assignments for catalog compatibility.
- Verification executed: `yarn typecheck` passed; `yarn dev` booted successfully (Vite served on `http://localhost:3001/`).

## DUB-337 — Root Family Stickers implementation
- Implemented `RootFamilyStickersGame` at `packages/web/src/games/reading/RootFamilyStickersGame.tsx` with `GameProps`, RTL-first sorting baskets, drag/tap immediate validation, icon-first controls (`▶`, `↻`, `💡`, `→`), adaptive slow-mode trigger, build-word step, phrase-read transfer step, and completion metrics.
- Added page shell `packages/web/src/pages/RootFamilyStickers.tsx` with optimistic sync-state behavior and parent-summary rendering using `parentDashboard.games.rootFamilyStickers.*` keys.
- Wired navigation and discovery: `App.tsx` route `/games/reading/root-family-stickers`, `Home.tsx` fallback mapping for slug `rootFamilyStickers`, and SEO route metadata + Hebrew SEO strings.
- Added migration `supabase/migrations/00014_seed_root_family_stickers.sql` (one `games` row, one `game_levels` row, plus age/topic/difficulty tag assignments with support age band `5-6`).
- Verification executed: `yarn dev` booted (Vite on `http://localhost:3001/`). `yarn typecheck` remains blocked by pre-existing `DecodableStoryReaderGame.tsx` i18n-typing errors unrelated to DUB-337 changes.

## DUB-354 — Sight Word Sprint implementation
- Implemented `SightWordSprintGame` at `packages/web/src/games/reading/SightWordSprintGame.tsx` with `GameProps`, icon-first controls, adaptive option scaling, soft timer (level 3 pacing), sentence-frame completion, and parent-summary completion metrics.
- Fixed a stale-closure regression in round intro callbacks by passing the active round into scheduled audio/phase transitions so replay/model prompts stay aligned with the current target word.
- Added page shell `packages/web/src/pages/SightWordSprint.tsx`, route wiring in `App.tsx` (`/games/reading/sight-word-sprint`), home discovery mapping (`sightWordSprint`), and SEO route metadata entries.
- Added DB seed migration `supabase/migrations/00015_seed_sight_word_sprint.sql` with one `games` row, one `game_levels` row, and age/topic/difficulty tag assignments.
- Verification executed: `yarn typecheck` passed; `PATH=\".venv-audio/bin:$PATH\" yarn generate-audio` passed (manifest refreshed); `yarn dev` booted successfully on `http://localhost:3001/`.

## DUB-59 — Shape Safari revalidation handoff
- Wake was assignment-driven on parent issue [DUB-59](/DUB/issues/DUB-59) after prior FED revalidation comments were already in thread.
- Re-checked current workspace behavior against blockers from [DUB-296](/DUB/issues/DUB-296) and [DUB-297](/DUB/issues/DUB-297); confirmed inactivity replay+highlight and required i18n/audio families are present.
- Verification in this heartbeat: `yarn typecheck` passed; `yarn dev --host 127.0.0.1 --port 4173` startup smoke passed.
- Updated parent issue to `in_review`, reassigned to [QA Engineer](/DUB/agents/qa-engineer), and posted QA closure-retest handoff comment.

## DUB-396 — Launch 6-7 star-message slot integration
- Checked out [DUB-396](/DUB/issues/DUB-396), posted kickoff + coordination note against [DUB-395](/DUB/issues/DUB-395), and implemented the FED lane using canonical slug `tamarWordTower` (CTO alias `star-message`).
- Updated handbook runtime in `InteractiveHandbookGame.tsx` to use `common.handbooks.tamarWordTower.*` narration/prompt/interaction key families, plus slot-specific parent next-step copy (`common.parentDashboard.handbooks.tamarWordTower.nextStep`).
- Updated page shell config in `InteractiveHandbook.tsx` to hydrate/persist progress by `handbookSlug='tamarWordTower'`, set `readingLadder.activeBook='book7'`, `defaultBand='6-7'`, and route cover metadata/audio to `handbooks.tamarWordTower.meta.*`.
- Verification in this heartbeat: `yarn typecheck` passed; `yarn dev` booted successfully (Vite at `http://localhost:3001/`).

## DUB-392 — Books 1/4/7 runtime gates generalized
- Refactored `packages/web/src/games/reading/InteractiveHandbookGame.tsx` to build page/interactions from `activeBookId` + `handbookSlug` instead of static `PAGE_DEFINITIONS`, with per-book quality gate defaults and slug-scoped completion/parent summary keys.
- Added runtime mapping support for launch trio slugs (`mikaSoundGarden`, `yoavLetterMap`, `tamarWordTower`) and removed remaining `magicLetterMap`-hardcoded key usage in game/page runtime.
- Updated `packages/web/src/pages/InteractiveHandbook.tsx` to resolve active ladder book from profile age-band and `readingLadder` config, hydrate/persist by resolved `handbooks.slug`, and pass dynamic level/game config (including intro audio path).
- Verification: `yarn workspace @dubiland/web typecheck`, `yarn typecheck`, `yarn dev --host 127.0.0.1 --port 4173` startup smoke.
- Posted implementation summary on [DUB-392](/DUB/issues/DUB-392) and marked task `done`.

## DUB-392 — Runtime gate threshold alignment follow-up
- Added explicit reading gate payload (`readingGate`) to `GameCompletionResult` and propagated it from `InteractiveHandbookGame` completion logic.
- Synced threshold messaging in runtime UI using i18n keys under `games.interactiveHandbook.gates.*` and ladder labels under `games.interactiveHandbook.ladderBooks.*`.
- Added migration `supabase/migrations/00018_handbook_reading_ladder_runtime_gates.sql` to store Books 1/4/7 `readingLadder` metadata + quality gate (`firstTryAccuracyMin=70`, `hintRateMax=35`) in `game_levels.config_json`.
- Verification in this heartbeat: `yarn typecheck` and `yarn dev --host 127.0.0.1 --port 3001` boot smoke.

## DUB-59 — QA contract revalidation after board reassignment
- Wake reason: `issue_assigned` with `PAPERCLIP_TASK_ID` set to [DUB-59](/DUB/issues/DUB-59).
- Checked out parent issue, reviewed latest QA blocker context from [DUB-372](/DUB/issues/DUB-372), and confirmed the live `ShapeSafariGame` already satisfies the four reported behavior gaps.
- Re-verified required i18n/audio families (`prompts.inactivity.*`, `hints.corners.*`, `hints.edges.*`, `recovery.demo.*`, `rewards.*`) exist in locale/audio paths.
- Verification in this heartbeat: `yarn typecheck` passed; `yarn dev --host 127.0.0.1 --port 4173` startup smoke passed.
- Patched [DUB-59](/DUB/issues/DUB-59) to `in_review`, reassigned to [QA Engineer](/DUB/agents/qa-engineer), and posted closure-retest handoff comment.

## DUB-125 — Counting Picnic replay icon compliance fix
- Checked out [DUB-125](/DUB/issues/DUB-125) after board reassignment and reviewed QA blocker note from [DUB-131](/DUB/issues/DUB-131).
- Applied a focused UI patch in `packages/web/src/games/numbers/CountingPicnicGame.tsx`: replaced 13 replay glyphs from `🔊` to `▶` on child-facing text rows and summary blocks.
- Confirmed no `🔊` glyph remains in replay affordances (`rg -n \"🔊\" ...` returns no matches).
- Verification in this heartbeat: `yarn typecheck` passed; `yarn dev --host 127.0.0.1 --port 4174` startup smoke passed.
- Patched [DUB-125](/DUB/issues/DUB-125) to `in_review`, reassigned to [QA Engineer 2](/DUB/agents/qa-engineer-2), and requested rerun.

## DUB-467 — Handbook renderer primitives closure
- Wake context `PAPERCLIP_TASK_ID` pointed to done backend issue [DUB-371](/DUB/issues/DUB-371), so heartbeat proceeded with assigned inbox lane [DUB-467](/DUB/issues/DUB-467) per Paperclip rules.
- Checked out [DUB-467](/DUB/issues/DUB-467), validated renderer scope in `packages/web/src/games/reading/HandbookPageRenderer.tsx` and integration in `packages/web/src/games/reading/InteractiveHandbookGame.tsx`.
- Confirmed v1 block types (`illustration`, `text`, `hotspot`, `badge`), soft-fail for unknown block types, deterministic order/stable keys, and interaction-id hotspot binding.
- Verification this heartbeat: `yarn typecheck` passed; `yarn dev --host 127.0.0.1 --port 4177` startup smoke passed.
- Posted completion update (100%) and marked [DUB-467](/DUB/issues/DUB-467) `done`, with unblock note for [DUB-469](/DUB/issues/DUB-469) and [DUB-452](/DUB/issues/DUB-452).

## DUB-373 — Parent dashboard trust fixes
- Implemented `ParentDashboard.tsx` updates to hide zeroed summary metrics when `children.length === 0` and show a focused empty state + `manageChildren` CTA.
- Aligned per-child `todayActivity` chip to real today-based math (`todayLearningMinutes` -> `%` of `DAILY_LEARNING_GOAL_MINUTES`) instead of 7-day active-day count.
- Removed placeholder audio-settings toggle from footer actions; kept only honest controls (`jumpToWeeklyProgress` when data exists + `manageChildren` always).
- Verification: `yarn dev --port 4175` startup smoke passed; `yarn typecheck` currently blocked by unrelated errors in `src/games/reading/InteractiveHandbookGame.tsx`.

## DUB-459 — Home handbook discoverability + bookshelf + celebration screen
- Checked out [DUB-459](/DUB/issues/DUB-459), posted CTO-required checkpoint comment with file scope and non-overlap against [DUB-441](/DUB/issues/DUB-441) and [DUB-458](/DUB/issues/DUB-458).
- Updated `packages/web/src/pages/Home.tsx`: ensured handbook card remains discoverable for age `3-4` and merged fallback game cards with catalog results by slug so missing catalog rows do not hide launch-critical cards.
- Updated `packages/web/src/pages/InteractiveHandbook.tsx`: added multi-book bookshelf selector UI, dedicated completion celebration screen (confetti/stars), and explicit replay/home actions with existing i18n/audio keys.
- Verification: `yarn typecheck` passed, `yarn build` passed, dev-server smoke passed on `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4299`.

## DUB-481 — Handbook startup critical-path render-first patch
- Checked out [DUB-481](/DUB/issues/DUB-481) and implemented render-first startup policy from [DUB-479](/DUB/issues/DUB-479) in the four scoped files.
- `ProtectedRoute` now allows `/games/reading/interactive-handbook` to render while auth is still loading, while keeping normal redirect behavior after auth resolution.
- `useAuth` now uses a handbook render-first path carve-out and lazy Supabase loading, keeping auth bootstrap active without forcing handbook spinner gating.
- `InteractiveHandbook.tsx` no longer imports Supabase at module top-level; hydrate/flush now lazy-load Supabase and preserve queued optimistic progress snapshots until `handbookId` resolves.
- `InteractiveHandbookGame.tsx` now applies startup narration policy: low-priority startup narration preload + two RAF paint-settled gate before initial narration, with no high-priority startup preload path.
- Verification this heartbeat: `yarn typecheck` passed; `yarn dev --port 4173` startup smoke passed (default port `3000` was occupied by another process).

## DUB-451 — Book 10 (`almaRootFamilies`) handbook integration
- Checked out [DUB-451](/DUB/issues/DUB-451), reconciled prior blocked state, and completed Book 10 runtime wiring in shared handbook files without reverting parallel-lane edits.
- `packages/web/src/pages/InteractiveHandbook.tsx`: added `book10`/`root-family-lab`/`almaRootFamilies` mappings, ladder metadata, bookshelf ordering/icon/style, guards, and default ladder config coverage.
- `packages/web/src/games/reading/InteractiveHandbookGame.tsx`: finalized Book 10 completion praise route (`patternSpotter`) and page narration sentence routing via `sentenceBank.pointedPhrases`.
- `packages/web/src/i18n/locales/he/common.json`: added `handbooks.almaRootFamilies` payload, ladder/choice keys, and parent-dashboard copy; validated JSON parse.
- Generated Hebrew audio with `scripts/generate-audio.py`; manifest entries now include Book 10 handbook + choices + parent dashboard paths.
- Verification this heartbeat: `yarn typecheck` passed; `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4173` startup smoke passed.
- Patched [DUB-451](/DUB/issues/DUB-451) to `done` with completion summary and verification notes.

## DUB-449 — Book 8 (`saharSecretClock`) handbook integration
- Checked out [DUB-449](/DUB/issues/DUB-449), posted kickoff coordination note, and implemented Book 8 in shared Interactive Handbook runtime without reverting parallel-lane edits.
- `packages/web/src/games/reading/InteractiveHandbookGame.tsx`:
  - Added `book8` ladder wiring, `secret-clock` alias mapping, and `saharSecretClock` slug support.
  - Added Book 8 quality gate defaults, prompt sequencing, narration routing, interaction flow, and new choice preset families (`time*`).
  - Added completion praise mapping to `scriptPackage.praise.timeDetective`.
- `packages/web/src/pages/InteractiveHandbook.tsx`:
  - Added `book8` + slug/alias/page-count mappings, shelf ordering/icon/style, and default ladder config entry.
- `packages/web/src/i18n/locales/he/common.json`:
  - Added `handbooks.saharSecretClock.*`, ladder label `games.interactiveHandbook.ladderBooks.book8`, `games.interactiveHandbook.choices.time*`, and `parentDashboard.handbooks.saharSecretClock.*`.
- Audio generation rerun via local venv:
  - `PATH=".venv-audio/bin:$PATH" python scripts/generate-audio.py`
  - Final result: `Done! 2435/2435 generated.` with manifest updated.
- Verification this heartbeat:
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4178` startup smoke passed.
- Patched [DUB-449](/DUB/issues/DUB-449) to `done` with completion summary and verification notes.

## DUB-446 — Book 5 (`naamaSyllableBox`) handbook integration
- Checked out [DUB-446](/DUB/issues/DUB-446), posted kickoff coordination note, and implemented Book 5 in shared handbook runtime with no reverts of parallel-lane edits.
- `packages/web/src/games/reading/InteractiveHandbookGame.tsx`:
  - Added `book5`/`syllable-box`/`naamaSyllableBox` mappings and ladder sequencing.
  - Added Book 5 interaction flow (`buildCV`, `pickDecodedWord`, `fixSyllableOrder`, `confusableSyllable`, `readShortPhrase`, `textToObject`, `transferBlend`, `literalQuestion`, `recapChoice`).
  - Added Book 5 choice preset families (`syllable_*`) and completion praise routing to `scriptPackage.praise.blendStrategy`.
- `packages/web/src/pages/InteractiveHandbook.tsx`:
  - Added Book 5 shelf/slug/alias/page-count wiring, icon/style class, and default ladder config entry.
- `packages/web/src/i18n/locales/he/common.json`:
  - Added `handbooks.naamaSyllableBox.*`, Book 5 ladder label, `games.interactiveHandbook.choices.syllable*`, and `parentDashboard.handbooks.naamaSyllableBox.*`.
- Audio generation rerun via local venv:
  - `PATH=".venv-audio/bin:$PATH" python scripts/generate-audio.py`
  - Final result: `Done! 2552/2552 generated.` and manifest includes Book 5 + syllable choice + parent dashboard paths.
- Verification this heartbeat:
  - `jq empty packages/web/src/i18n/locales/he/common.json` passed.
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4178` startup smoke passed.

## DUB-426 — More Or Less Market transition wrapper parity
- Checked out [DUB-426](/DUB/issues/DUB-426) and posted kickoff with scoped file ownership (`packages/web/src/App.tsx`).
- Fixed route transition inconsistency by wrapping `/games/numbers/more-or-less-market` in `withAnimatedPage(<MoreOrLessMarket />, 'app')`, matching peer game routes.
- Verification this heartbeat:
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4180` startup smoke passed.

## DUB-500 — Home UX overhaul closeout
- Finalized FED3 delta on home-page overhaul by adding featured-helper copy and per-section game-count messaging in `packages/web/src/pages/Home.tsx`.
- Added matching Hebrew i18n entries in `packages/web/src/i18n/locales/he/common.json` and regenerated audio artifacts/manifest entries under `packages/web/public/audio/he/home/*` and `packages/web/public/audio/he/manifest.json`.
- Verification this heartbeat:
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4182` startup smoke passed.
  - `yarn typecheck` failed due pre-existing unrelated error in `packages/web/src/hooks/audioPlaybackQueue.ts` (`TS2322` on `HTMLAudioElement` → `AudioLike`), outside DUB-500 file scope.

## DUB-527 — Decodable age-band scaling closure verification
- Wake reason targeted [DUB-527](/DUB/issues/DUB-527); issue was `in_review` but assigned to FED3, so checkout resumed it to `in_progress` and allowed final verification.
- Re-verified runtime implementation already present in:
  - `packages/web/src/games/reading/DecodableStoryReaderGame.tsx` (age-band packs, decode-first gating, per-band thresholds, parent metrics payload)
  - `packages/web/src/pages/DecodableMicroStories.tsx` (runtime age-band pass-through + band-aware parent summary rendering)
- Verification this heartbeat:
  - `yarn --cwd packages/web typecheck` passed.
  - `yarn --cwd packages/web dev --host 127.0.0.1 --port 4173` boot smoke passed (`VITE v6.4.2 ready`).
- Posted completion comment and patched [DUB-527](/DUB/issues/DUB-527) to `done`.
