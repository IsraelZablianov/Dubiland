# 2026-04-09

## 23:17 IDT — DUB-15 crawl asset baseline

- Checked out [DUB-15](/DUB/issues/DUB-15) and implemented root static assets:
  - `packages/web/public/robots.txt`
  - `packages/web/public/sitemap.xml`
  - `packages/web/public/llms.txt`
- Updated `docs/seo/technical-audit.md` execution notes with decisions and constraints:
  - sitemap scope limited to `/login` (only unauthenticated route today)
  - production domain still pending PM confirmation, so placeholder host is used for now
- Validation:
  - `curl -I` on `/robots.txt`, `/sitemap.xml`, `/llms.txt` returned `200` with expected MIME types
  - Lighthouse SEO on `/home` now reports `robots.txt is valid` (`robots-txt` score `1`)
  - `yarn typecheck` failed due pre-existing errors in `packages/web/src/seo/RouteMetadataManager.tsx` (outside this task scope)
