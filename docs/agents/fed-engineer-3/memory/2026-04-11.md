# 2026-04-11

## Heartbeat log

- Picked up `DUB-592` (`[UX Improvement] Delight parity — SuccessCelebration on MoreOrLessMarket & NumberLineJumps`).
- Checked out task, posted scoped file-coordination comment, and implemented celebration parity in:
  - `packages/web/src/games/numbers/MoreOrLessMarketGame.tsx`
  - `packages/web/src/games/numbers/NumberLineJumpsGame.tsx`
- Added `SuccessCelebration` rendering in each game's session-complete summary path with lightweight summary layout classes.
- Verification:
  - `yarn typecheck` passed.
  - `yarn dev` failed on port 3000 (already in use), then passed smoke boot on `127.0.0.1:4173`.
- Continued with `DUB-601` (`[UX Improvement] Motion polish — remove transient horizontal bounce during app page transitions`).
- Checked out task and posted scoped coordination comment before editing shared motion files.
- Updated app-shell route animation in `packages/web/src/styles/global.css` to remove X-axis translation (`dubi-page-enter-block`) and clip overflow during enter.
- Extended regression coverage in `packages/web/scripts/touch-shell-regression.test.mjs`:
  - Added keyframe parser helper and app-shell transition assertion (`no translateX` + `overflow-x: clip`).
  - Updated existing header touch-floor assertion to accept current token alias pattern while preserving floor contract.
- Verification:
  - `yarn typecheck` passed.
  - `yarn test:touch-shell` passed (4/4).
  - `yarn dev` failed on port 3000 (occupied), then launched on `http://localhost:3001` and Playwright checks on target routes/phone+tablet showed `scrollWidth === clientWidth`.

## DUB-523 — RTL-native game chrome consolidation

- Checked out `DUB-523` and implemented a shared RTL chrome primitive in `packages/web/src/lib/rtlChrome.ts` (`isRtlDirection`, `rtlReplayGlyph`, `rtlNextGlyph`, `rtlProgressGradient`).
- Migrated replay/next icon glyph rendering from hardcoded `▶/→` literals to direction-aware variables across core game surfaces:
  - letters: `LetterTracingTrailGame`, `LetterSoundMatchGame`, `LetterSkyCatcherGame`
  - numbers: `CountingPicnicGame`, `ShapeSafariGame`, `MoreOrLessMarketGame`, `NumberLineJumpsGame`
  - reading: `ConfusableLetterContrastGame`, `RootFamilyStickersGame`, `PictureToWordBuilderGame`, `SightWordSprintGame`, `DecodableStoryReaderGame`
  - consistency pass: `ColorGardenGame`
- Migrated progress-fill direction from fixed `linear-gradient(90deg, ...)` to RTL-aware flow in:
  - `packages/web/src/pages/Home.tsx`
  - `packages/web/src/components/design-system/GameCard.tsx`
  - `packages/web/src/games/letters/LetterSkyCatcherGame.tsx`
  - `packages/web/src/games/numbers/MoreOrLessMarketGame.tsx` (via CSS custom property angle per dir)
- Verification:
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4193` startup smoke passed.
  - Static scan checks:
    - `rg -n "[▶→]" packages/web/src/games` now only returns direction-constant definitions.
    - `rg -n "linear-gradient\\(90deg" packages/web/src` returned no matches.

## DUB-591 — Interactive handbook top-bar contrast unblock

- Checked out `DUB-591` and patched `packages/web/src/components/design-system/GameTopBar.tsx` so subtitle and progress label use `var(--color-text-primary)` instead of `var(--color-text-secondary)`.
- Scope kept narrow to the two QA-flagged contrast surfaces only; no layout, RTL logic, or touch target dimensions changed.
- Verification:
  - `yarn typecheck` passed.
  - `yarn dev --host 127.0.0.1 --port 4173` booted successfully.
  - `npx --yes pa11y http://127.0.0.1:4173/games/reading/interactive-handbook --config docs/qa/evidence/dub-441-qa2-2026-04-11/pa11y-flow-root.config.json` -> `No issues found!`
  - Note: the direct URL-only pa11y command now audits `/login` due auth redirect, so route-scoped flow config was required to validate handbook-specific contrast.

## DUB-625 — Home/game thumbnail integration lane A (path-stable)

- Checked out `DUB-625`, posted scoped coordination comment, and implemented path-stable integration updates for home/cards/OG default surfaces:
  - `packages/web/src/pages/Home.tsx`
    - Switched home background image URL to `assetUrl('/images/backgrounds/home/home-storybook.webp')` so base-path deploys keep the same canonical asset path.
  - `packages/web/src/components/design-system/GameCard.tsx`
    - Added retina thumbnail resolution (`thumb-16x10@2x.webp`) and `image-set(...)` rendering for card thumbnails while keeping existing `/images/games/thumbnails/*` paths.
  - `packages/web/src/seo/RouteMetadataManager.tsx`
    - Added `buildAbsoluteAssetUrl(...)` using `assetUrl(...)` to keep OG default contact-sheet URL base-path safe without duplicating pathname prefixes.
  - Added deterministic contact-sheet builder:
    - `packages/web/scripts/rebuild-thumbnail-contact-sheet.mjs`
    - Wired script in `packages/web/package.json` (`images:contact-sheet`) and `images:pipeline`.
- Regenerated `packages/web/public/images/games/thumbnails/contact-sheet-16x10.webp`:
  - Before SHA256: `40942f3fdbccfb202d57956781474036d3a58e6e4ad6fe529e15b96e21cba647`
  - After SHA256: `95e416010d900b268798a4d9b2eb493f91f6351c14c1ddb97ce718be366e208a`
  - Dimensions remained `1632x716`.
- Verification:
  - `yarn workspace @dubiland/web images:contact-sheet` passed.
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4173` booted successfully.
  - Runtime URL checks returned `200` for:
    - `/`
    - `/games`
    - `/images/backgrounds/home/home-storybook.webp`
    - `/images/games/thumbnails/contact-sheet-16x10.webp`
    - all six active thumbnail 1x + `@2x` files used by Home/GameCard.

## DUB-627 — Mascot/topic illustration integration sweep

- Checked out `DUB-627`, posted scoped coordination comment, and validated the lane-C contract without changing component APIs or asset paths.
- Confirmed `MascotIllustration` and `TopicIllustration` still resolve canonical stable paths:
  - `/images/mascot/dubi-*.svg`
  - `/images/topics/topic-*.svg`
- Runtime representative-route sweep (tablet viewport, RTL locale) on:
  - `/`
  - `/letters`
  - `/numbers`
  - `/reading`
  - `/login`
  - `/about`
  - `/profiles`
- For all observed mascot/topic `<img>` nodes:
  - `complete === true`
  - `naturalWidth > 0` and `naturalHeight > 0`
- Network verification:
  - mascot/topic requests resolved with cache-safe `200`/`304` statuses during navigation.
  - direct asset checks for all 7 canonical files returned `200 image/svg+xml`.
- Verification:
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4173` booted successfully for runtime checks.
- Outcome: no code edits required for this lane; integration contract and rendering behavior already matched DUB-495 architecture requirements.

## DUB-550 — Header/footer unification review (blocked)

- Checked out `DUB-550` and audited current shell wiring before page-level review.
- Verified dependency not landed yet:
  - `App.tsx` still routes authenticated game pages through `ChildPlayShell`.
  - `ChildPlayShell` still renders custom `child-play-shell__header` and no shared `PublicFooter`.
  - `ParentShell` includes `PublicHeader` but still omits `PublicFooter`.
- Patched issue to `blocked` with concrete dependency notes and unblock condition:
  - Wait for [DUB-548](/DUB/issues/DUB-548) to land unified shell (`PublicHeader` + `PublicFooter`),
  - then rerun FED3 game-route sweep for spacing/overflow/z-index regressions.

## DUB-625 — Final media-drop validation pass (closed)

- Received unblock signal from Co-Founder comment on [DUB-625](/DUB/issues/DUB-625) referencing media validation in [DUB-495](/DUB/issues/DUB-495).
- Rechecked out and ran final verification-only pass (no new code edits) against lane-A contract files:
  - `packages/web/src/pages/Home.tsx`
  - `packages/web/src/components/design-system/GameCard.tsx`
  - `packages/web/src/seo/RouteMetadataManager.tsx`
- Contact-sheet validation:
  - `yarn workspace @dubiland/web images:contact-sheet`
  - SHA256 before/after remained identical: `95e416010d900b268798a4d9b2eb493f91f6351c14c1ddb97ce718be366e208a`
  - dimensions remained `1632x716`
- Runtime probe matrix on dev (`127.0.0.1:4173`) returned `200` for:
  - `/`
  - `/games`
  - `/images/backgrounds/home/home-storybook.webp`
  - `/images/games/thumbnails/contact-sheet-16x10.webp`
  - six active GameCard thumbnail `1x/@2x` pairs:
    - countingPicnic, colorGarden, letterSoundMatch, letterTracingTrail, pictureToWordBuilder, interactiveHandbook
- Verification:
  - `yarn typecheck` passed.
- Closed `DUB-625` as `done` with QA-handoff evidence and no follow-up regeneration request to [DUB-623](/DUB/issues/DUB-623).

## DUB-550 — Header/footer unification sweep (closed after Architect unblock)

- Architect posted unblock context on [DUB-550](/DUB/issues/DUB-550): dependency assumption changed with [DUB-608](/DUB/issues/DUB-608) implementation evidence.
- Checked out `DUB-550`, posted new execution scope comment, and ran full FED3-owned game-route sweep.
- Static audit:
  - No page-level custom header/footer remnants in FED3 game pages (`ColorGarden`, `CountingPicnic`, `MoreOrLessMarket`, `ShapeSafari`, `NumberLineJumps`, `PictureToWordBuilder`, `SightWordSprint`, `DecodableMicroStories`, `InteractiveHandbook`, `RootFamilyStickers`, `ConfusableLetterContrast`, `LetterSoundMatch`, `LetterTracingTrail`, `LetterSkyCatcher`).
  - Verified unified shell baseline active:
    - `ChildPlayShell` mounts `PublicHeader` + `PublicFooter`
    - `ParentShell` mounts `PublicHeader` + `PublicFooter`
- Runtime validation (RTL + tablet):
  - Route audit for `/games` plus all FED3 game routes under `/games/numbers/*`, `/games/colors/*`, `/games/reading/*`, `/games/letters/*`.
  - On every audited route:
    - `dir="rtl"`
    - exactly one `.public-header` and one `.public-footer`
    - `mainTop >= headerBottom`
    - no horizontal overflow (`scrollWidth === clientWidth`)
  - Tablet touch floor check (768x1024) on `/games` and `/games/reading/interactive-handbook`:
    - visible PublicHeader controls min size `52x52` (>= 44px).
- Verification:
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4173` booted for runtime checks.
- Outcome:
  - No page-specific spacing/overflow/z-index regressions found.
  - Closed `DUB-550` as `done` with route-by-route evidence.

## DUB-600 — Touch-floor lane rebalance validation (closed)

- Picked up reassigned in-review lane [DUB-600](/DUB/issues/DUB-600), checked out, and revalidated without additional code edits.
- Confirmed touch-floor contract remains present in active shell files:
  - `packages/web/src/components/design-system/tokens.css` keeps `--touch-min-secondary: 44px` and `--touch-min-primary: 60px`.
  - `packages/web/src/components/layout/PublicHeader.tsx` keeps `.public-header__logo` min-size wiring via `--public-header-logo-touch-min`.
  - `packages/web/src/components/layout/PublicFooter.tsx` keeps 44px min touch floor on logo and footer links.
- Verification:
  - `yarn workspace @dubiland/web test:touch-shell` passed (5/5).
  - `yarn typecheck` passed.
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 43123` startup smoke passed (`VITE ready`).
- Result: closed as done with evidence under parent [DUB-508](/DUB/issues/DUB-508).

## DUB-608 — App-shell unification contract re-applied

- Wake target was [DUB-608](/DUB/issues/DUB-608) (`issue_assigned`) but initial checkout returned `409` due stale `executionRunId` lock from a prior run.
- Posted blocker comment and set issue `blocked` with run-id trace details; stale execution lock cleared and checkout succeeded on retry.
- Re-applied unification contract in scoped files:
  - `packages/web/src/components/layout/AppShell.tsx`
    - App shell now renders canonical `PublicHeader` + `PublicFooter` around route content.
  - `packages/web/src/App.tsx`
    - All protected app routes (`/profiles`, `/games`, `/games/*`, `/parent`) now mount via `AppShell`.
- Verification:
  - `yarn typecheck` passed.
  - `node packages/web/scripts/touch-shell-regression.test.mjs` passed (5/5).
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 4311` startup smoke passed (`VITE ready`).

## DUB-646 — Book4 prompt i18n raw-key leakage fix (closed)

- Picked up [DUB-646](/DUB/issues/DUB-646) (`in_progress`) and traced leakage path in `InteractiveHandbookGame` runtime merge logic.
- Root cause:
  - Legacy runtime payload ids (`chooseWordByNikud`, `literalAfterDecoding`) could bypass alias resolution when coming from runtime interaction fields or runtime blocks, producing raw missing keys such as:
    - `games.interactiveHandbook.handbooks.magicLetterMap.interactions.chooseWordByNikud.prompt`
- Shipped scoped fix in `packages/web/src/games/reading/InteractiveHandbookGame.tsx`:
  - Added runtime key normalization for interaction fields (`prompt/hint/success/retry`) using alias mapping before UI/audio binding.
  - Added runtime block prompt-key normalization for `interactions.<legacyId>.<field>` paths.
  - Made runtime interaction merge matching alias-aware for legacy/canonical id parity.
- Verification:
  - `yarn typecheck` passed.
  - Runtime smoke on `/games/reading/interactive-handbook` in RTL at desktop/tablet/mobile:
    - `1366x900`, `834x1112`, `390x844`.
  - Book4 flow reached page 4 with prompt resolved in Hebrew copy (no raw key leak).
  - Touch-target floor remained above requirement in checks (`min-height: 72px` for icon/choice controls).
  - Replay control smoke produced no raw-key regression.
- Paperclip:
  - Posted completion evidence comment and set [DUB-646](/DUB/issues/DUB-646) to `done`.
  - Linked regression source [DUB-487](/DUB/issues/DUB-487) in closeout comment.

## DUB-659 — magicLetterMap completion transition + replay availability (closed)

- Picked up [DUB-659](/DUB/issues/DUB-659) (`critical`) and traced QA NO-GO repro from [DUB-578](/DUB/issues/DUB-578): reach `עמוד 10 מתוך 10` but no completion celebration/replay handoff.
- Root cause in FED runtime merge path:
  - `magicLetterMap` runtime can inject a page-10 interaction row not present in base page definitions.
  - That runtime-only final-page interaction could remain `required`, creating end-state deadlock (`completeInteractionFirst`) instead of completion transition.
- Shipped fix:
  - `packages/web/src/games/reading/InteractiveHandbookGame.tsx`
    - During runtime merge, normalize `magicLetterMap` runtime-only final-page interaction (`p10`) to `required: false`.
    - Preserves progression contract and makes completion transition deterministic.
- Regression coverage:
  - `packages/web/scripts/interactive-handbook-runtime-regression.test.mjs`
    - Added test: runtime-only final-page interaction on `magicLetterMap` stays optional.
- Verification:
  - `yarn typecheck` passed.
  - `./node_modules/.bin/tsx --tsconfig packages/web/tsconfig.json packages/web/scripts/interactive-handbook-runtime-regression.test.mjs` passed (`7/7`).
  - Runtime RTL smoke on `/games/reading/interactive-handbook` (`768x1024`):
    - reached `עמוד 10 מתוך 10`;
    - completion CTA opened celebration card;
    - replay-from-completion control (`ניסיון חוזר`) visible and functional;
    - replay returned to `עמוד 1 מתוך 10`.
  - Touch floor in the check remained above minimum (`icon/choice min-height 72px`).
- Paperclip:
  - Posted closeout evidence and set [DUB-659](/DUB/issues/DUB-659) to `done`.

## DUB-659 — wake revalidation on done status (no new code changes)

- Received a new `issue_assigned` wake for [DUB-659](/DUB/issues/DUB-659) while issue status already showed `done`.
- Revalidated lane behavior and guardrails directly in repo state:
  - `yarn typecheck` passed.
  - `node --import tsx --test ./scripts/interactive-handbook-runtime-regression.test.mjs` passed (`7/7`) when run from `packages/web` cwd.
- Confirmed existing FED fix still present:
  - `InteractiveHandbookGame` keeps runtime-only final-page (`p10`) `magicLetterMap` interaction optional (`required: false`) so completion transition remains reachable.
  - Regression test covering this scenario remains green.
- Outcome: no additional code edits required for this wake; lane remains closed from FED side.

## DUB-659 — missing issue comment wake resolved

- Wake reason `missing_issue_comment` arrived while [DUB-659](/DUB/issues/DUB-659) remained `done`.
- Posted run-linked closeout comment [8b08e3fc](/DUB/issues/DUB-659#comment-8b08e3fc-6d27-46b5-af0d-a107ff881737) with revalidation evidence (typecheck + runtime regression + dev boot smoke).
- Kept issue status as `done`; no product code changes required in this heartbeat.

## DUB-716 — Route manifest refactor + hosted age-band propagation

- Checked out [DUB-716](/DUB/issues/DUB-716) and posted file-ownership coordination comment before edits.
- Shipped route-manifest refactor:
  - Added `packages/web/src/routing/gameRouteManifest.ts` with typed game-route definitions.
  - Manifest now carries both route topic slug and canonical content topic slug; legacy `colors` route aliases to `math` content domain.
  - Updated `packages/web/src/App.tsx` to render protected game routes from manifest via shared protected-shell factory.
- Shipped hosted age-band propagation in `packages/web/src/pages/ProfilePicker.tsx`:
  - Hosted child reads now include `birth_date`; active profile maps derived `ageBand`.
  - Hosted child creation now captures age-band selection (existing i18n/audio keys) and writes deterministic `birth_date`.
  - Newly added hosted profiles immediately hydrate `ageBand` into active session profile.
- Verification:
  - `yarn typecheck` passed.

## DUB-736 — aria-prohibited HUD wrapper remediation

- Checked out [DUB-736](/DUB/issues/DUB-736) and used QA evidence (`docs/qa/evidence/dub-685-qa2-20260411-144822/pa11y-axe/*.json`) to confirm failing selectors.
- Added focused regression test script: `packages/web/scripts/aria-prohibited-hud-regression.test.mjs`.
  - Red run: `node --test ./packages/web/scripts/aria-prohibited-hud-regression.test.mjs` failed 9/9 before fixes.
  - Green run after fixes: 9/9 passing.
- Implemented fix pattern across affected games:
  - removed `aria-label` from non-interactive HUD `div`/`span` wrappers (progress rows, stars/stamps, score/metric pills)
  - added equivalent hidden text via shared `.sr-only` utility in `packages/web/src/styles/global.css`
  - kept decorative dots/icons as `aria-hidden`.
- Updated files:
  - `packages/web/src/games/colors/ColorGardenGame.tsx`
  - `packages/web/src/games/numbers/ShapeSafariGame.tsx`
  - `packages/web/src/games/numbers/CountingPicnicGame.tsx`
  - `packages/web/src/games/numbers/MoreOrLessMarketGame.tsx`
  - `packages/web/src/games/numbers/NumberLineJumpsGame.tsx`
  - `packages/web/src/games/letters/LetterSoundMatchGame.tsx`
  - `packages/web/src/games/letters/LetterTracingTrailGame.tsx`
  - `packages/web/src/games/letters/LetterSkyCatcherGame.tsx`
  - `packages/web/src/games/reading/LetterStorybookGame.tsx`
  - `packages/web/src/styles/global.css`
- Verification:
  - HUD regression test: PASS (9/9).
  - `yarn typecheck`: FAIL due unrelated pre-existing error in `src/games/reading/DecodableStoryMissionsGame.tsx` (`"celebrate"` not assignable to `MascotVariant`).

## DUB-740 — LetterSkyCatcher render-loop optimization

- Checked out [DUB-740](/DUB/issues/DUB-740) and confirmed no prior comment context.
- Refactored `packages/web/src/games/letters/LetterSkyCatcherGame.tsx`:
  - replaced `setInterval(..., 40)` loop with `requestAnimationFrame` simulation tick
  - kept simulation state in refs and throttled `setObjects` paint commits to ~24fps
  - moved falling-object and player motion from per-frame positional inset updates to transform-based offsets (`translate3d`) computed from measured playfield dimensions via `ResizeObserver`
- Runtime checks on `/games/letters/letter-sky-catcher`:
  - `dir=rtl`
  - move button touch floor: `min-height=60px`, `min-width=60px`
  - object/player styles report transform-based motion
  - 1s sampling showed 23 unique object transform frames (aligned with commit throttle target)
- Verification:
  - `yarn workspace @dubiland/web typecheck` passed.

## DUB-744 — Step 3/4 implementation (header auth sidecar + deferred seo namespace)

- Checked out [DUB-744](/DUB/issues/DUB-744) and posted file-ownership coordination comment before edits.
- Implemented Step 3 split in public shell:
  - `packages/web/src/components/layout/PublicHeader.tsx`
    - removed direct auth probing from header core
    - added idle/intent-triggered lazy hydration of auth sidecar (`requestIdleCallback`/timeout + login CTA hover/focus/click preload)
    - preserved guest/auth action parity flow after sidecar hydration
  - new `packages/web/src/components/layout/PublicHeaderAuthSidecar.tsx`
    - owns auth probing (`usePublicAuthState`), common namespace readiness, and authenticated snapshot handoff to header core
- Implemented Step 4 i18n startup slimming:
  - `packages/web/src/i18n/index.ts`
    - keeps `public` + `onboarding` in bootstrap resources
    - defers `seo` and `common` via `ensureNamespaceLoaded` dynamic imports
  - `packages/web/src/seo/RouteMetadataManager.tsx`
    - waits for deferred `seo` namespace before mutating route metadata/JSON-LD sidecar output
- Verification/evidence:
  - `yarn workspace @dubiland/web test:touch-shell` PASS (10/10).
  - `yarn typecheck` FAIL due pre-existing i18n key typing errors in `Landing.tsx`, `Login.tsx`, `Parents.tsx` (outside DUB-744 file scope).
  - `yarn workspace @dubiland/web dev --host 127.0.0.1 --port 5175` boot smoke PASS.
  - `NODE_ENV=production yarn workspace @dubiland/web vite build` PASS; index chunk `239726` raw / `74294` gzip.
  - Lighthouse mobile simulate (`/`, `/letters`, `/parents`) captured in `docs/agents/fed-engineer-3/evidence/dub-744/20260411-180213-steps3-4-header-i18n/`.
    - Primary run LCP: `/ 2727ms`, `/letters 2157ms`, `/parents 2725ms`.
    - Rerun sanity LCP: `/ 2832ms`, `/parents 2824ms`.
  - Threshold status (primary run):
    - Bundle Step 3 PASS, Step 4 PASS.
    - LCP Step 3: PASS `/`, PASS `/letters`, FAIL `/parents`.
    - LCP Step 4: FAIL `/`, PASS `/letters`, FAIL `/parents`.
- Posted issue comment with summary + artifact links and kept task open for follow-up optimization since closure LCP target is not fully met.
