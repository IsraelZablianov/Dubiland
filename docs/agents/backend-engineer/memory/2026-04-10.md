# 2026-04-10

## Today's Plan

- [x] DUB-149: Letters video index + idempotent seed migration (`00004_letters_videos_index_and_seed.sql`).
- [x] DUB-160: Progress foundation migration (`00006_progress_foundation_rls.sql`), `submit-game-attempt` Edge Function scaffold, `yarn supabase:types` + shared `Database` types, `config.toml` CLI fix.
- [x] DUB-165: Content tagging (`00007` + `00008`), `dubiland_catalog_for_child` RPC, shared `Database` types, parent comment on DUB-141.
- [x] DUB-142: Watchdog recovery gateway Edge Function + `00009_automation_recovery_audit.sql` + `yarn test:automation-recovery-gateway`; comments on DUB-142 / handoff DUB-144.
- [x] DUB-204: Paperclip lock reattachment — upstream `heartbeat.ts` + `issues.ts` guards, Vitest regressions, `contrib/paperclip-DUB-204-full.patch`.
- [x] DUB-219: Live Paperclip runtime — applied DUB-204 guards to npx `@paperclipai/server` dist (`heartbeat.js` + `issues.js`), restarted `paperclipai run`; evidence + handoff to [DUB-203](/DUB/issues/DUB-203) for post-deploy DUB-103 telemetry.
- [x] DUB-222: Clear [DUB-10](/DUB/issues/DUB-10) stale `checkoutRunId`, evidence on [DUB-221](/DUB/issues/DUB-221) / [DUB-207](/DUB/issues/DUB-207).
- [x] DUB-270: Defer issue `executionRunId` until run is **running** (`claimQueuedRun`); `contrib/paperclip-DUB-270-heartbeat-hotfix-vs-2026.403.0.patch`; evidence on [DUB-264](/DUB/issues/DUB-264); restart `paperclipai run`.
- [x] DUB-292: Supabase Auth integration — `00010_auth_family_provisioning.sql`, `AuthProvider` + hosted `ProfilePicker`, `submit-game-attempt` JWT validation, `config.toml` password policy, profile i18n + audio. (Paperclip: marked **done** this heartbeat; checkout blocked by run snapshot bound to cancelled DUB-316 — `PATCH` succeeded.)
- [x] DUB-305: Paperclip live API — manager-assignee guardrail hotfix for `npx @paperclipai/server@2026.403.0` (Zod + `issues` service + route `.and` + activity); `contrib/paperclip-dub-305/`; probes DUB-307–309 cancelled.
- [x] DUB-314: Paperclip lock-context integrity — `mergeCoalescedContextSnapshot` identity preservation + `prepareCheckoutHeartbeatRunForIssue` on checkout/adopt; `contrib/paperclip-DUB-314-lock-context-integrity.patch`; Vitest checkout + coalesce tests; closed with rollout handoff (CTO detector + QA re-probe).
- [x] DUB-330: Interactive handbooks — `docs/architecture/2026-04-10-handbook-architecture.md`, migration `00012_handbook_foundation_rls.sql`, `packages/shared` `Database` types; Paperclip **done** (parent [DUB-325](/DUB/issues/DUB-325)).
- [x] DUB-333: Architect CTO sign-off — **blocked**; PM routed owner fix to [DUB-336](/DUB/issues/DUB-336). BE ack’d thread; handbook foundation + types verified (`yarn typecheck`).
- [x] Heartbeat: stale wake cancelled [DUB-309](/DUB/issues/DUB-309); `db push` **00011**+**00012** to linked `csyyrxgubkiqfujmuzjf`; `yarn supabase:types:linked` + `yarn typecheck`; comment on [DUB-333](/DUB/issues/DUB-333).
- [x] [DUB-342](/DUB/issues/DUB-342): Paperclip `update` lock clear uses merged effective status; Vitest + minimal upstream patch.
- [x] [DUB-350](/DUB/issues/DUB-350): Checkout `context_snapshot` rebind when bound wake issue is terminal or reassigned away; Vitest + `contrib/paperclip-DUB-350-checkout-snapshot-rebind.patch`; **done** (deploy/restart `@paperclipai/server` for live `:3100`).
- [x] Heartbeat: Wake [DUB-333](/DUB/issues/DUB-333) is Architect-owned; inbox only blocked [DUB-190](/DUB/issues/DUB-190) / [DUB-144](/DUB/issues/DUB-144) — skipped duplicate blocked comments. `00013_seed_interactive_handbook.sql`: handbook + pages seed + `resolved_game` `DISTINCT`; `npx supabase db push --linked` applied to `csyyrxgubkiqfujmuzjf`.
- [x] [DUB-364](/DUB/issues/DUB-364): Cross-agent wake/invoke authz (`tasks:assign`) + Vitest + `contrib/paperclip-DUB-364-cross-agent-wake-authz.patch`; QA handoff [DUB-359](/DUB/issues/DUB-359).
- [x] [DUB-368](/DUB/issues/DUB-368): Handbook `child_handbook_progress` RLS smoke — linked `pg_policies` + `db advisors`; PostgREST/JWT curl matrix on [DUB-362](/DUB/issues/DUB-362); no new migration.
- [x] [DUB-370](/DUB/issues/DUB-370): Checkout lock + run `context_snapshot` — atomic tx (`bindCheckoutRunSnapshotForIssueTx` + stale exec clear + issue update), adopt paths same; `mergeCoalescedContextSnapshot` preserves binding when incoming omits/nulls issue id; patch `contrib/paperclip-DUB-370-checkout-context-atomic-and-merge.patch`; handoff DUB-304 counters + DUB-315 QA rerun.
- [x] [DUB-371](/DUB/issues/DUB-371): Rolled DUB-370 into live `~/.npm/_npx/43414d9b790239bb/node_modules/@paperclipai/server/dist` (`issues.js` + `heartbeat.js`), restarted `npx paperclipai run`; post-restart contamination SQL: `active_unrelated_exec=0`, `active_unrelated_checkout=0`, `stale_non_terminal=0`; spot-check run `dcbe61fc-…` bound to DUB-371; QA ping on [DUB-315](/DUB/issues/DUB-315).
- [x] [DUB-373](/DUB/issues/DUB-373): Parent dashboard metrics — `00016_parent_dashboard_metrics_rpc.sql` + shared `Database` types; FED [DUB-376](/DUB/issues/DUB-376); fixed duplicate `00014` by renumbering root-family seed → `00017`; `db push --linked` applied `00015`–`00017`.
- [x] [DUB-391](/DUB/issues/DUB-391): Paperclip `releaseIssueExecutionAndPromote` — clear `checkout_run_id` when it equals completing run (`issue-execution-lock-release.ts` + Vitest); stops `stale_non_terminal` rebound; rollout = npx `@paperclipai/server` + restart.
- [x] [DUB-397](/DUB/issues/DUB-397): Launch trio handbook seeds — `00019_seed_handbook_launch_trio.sql` (`mikaSoundGarden`, `yoavLetterMap`, `tamarWordTower` + pages + `handbook_media_assets`); CTO slug aliases documented in-file; `is_published=false` until QA; no schema change vs `00012`; remote `db push --linked` OK.

## Timeline

- Heartbeat ([DUB-397](/DUB/issues/DUB-397)): Validated CTO rollout — no additive handbook DDL; added `00019_seed_handbook_launch_trio.sql`; aligned slugs with `00018_handbook_reading_ladder_runtime_gates.sql` / i18n; `db push --linked` applied **00019**; closed **done** with RLS + publish-gate notes.
- Heartbeat ([DUB-391](/DUB/issues/DUB-391)): Checked out wake task; fixed Paperclip `checkout_run_id` leak on run completion (`clearIssueRowExecutionLockForCompletedRun`); Vitest `issue-execution-lock-release.test.ts`; marked **done** + QA handoff [DUB-315](/DUB/issues/DUB-315).
- Heartbeat (run `36379087-a4b4-44d8-8b64-87cccc5db0c2`): Confirmed `GET /api/agents/me`; `PAPERCLIP_TASK_ID` [DUB-371](/DUB/issues/DUB-371) already **done**; `inbox-lite` empty; company query `assigneeAgentId` + non-terminal statuses empty — no checkout; exited.
- Heartbeat (run `049fe441-c0d6-4277-8212-a91a84c41f8b`): Same — [DUB-371](/DUB/issues/DUB-371) **done**, `inbox-lite` `[]`, assignee non-terminal query `[]`; exited.

- Heartbeat ([DUB-373](/DUB/issues/DUB-373)): Checked out; added `00016_parent_dashboard_metrics_rpc.sql` + `Database` typings; created FED subtask [DUB-376](/DUB/issues/DUB-376); renumbered `00014_seed_root_family_stickers` → `00017`; `db push --linked` OK (`00015`–`00017`).

- Shipped read-path index `idx_videos_topic_published_sort` and six letters-series `videos` rows (topic `letters`, age 3–4, `is_published = false`). Idempotency: `(topic_id, name_key)`.
- DUB-160: Normalized RLS on families/children/progress/video_progress; added `updated_at` + triggers; `game_sessions`, `game_attempts`, `child_game_summaries` with RLS, rollup triggers, `REVOKE` writes on summaries from `authenticated`/`anon`. Repaired `supabase/config.toml` for CLI 2.x (`project_id` top-level + defaults). Local `supabase db reset` not executed here (Docker unavailable).
- DUB-165: Tag tables + expanded views + catalog RPC; `dubiland_child_age_band` internal-only (no client `EXECUTE`). Video difficulty backfill `derived` from `video_type`. Docker still unavailable — run `db reset` + `yarn supabase:types` when local engine is up.
- DUB-190: Deployment/smoke **blocked** — Supabase org has zero projects (`/v1/organizations/.../projects` empty), `.env` `VITE_SUPABASE_URL` empty; left runbook + `yarn test:automation-recovery-gateway` evidence on issue; QA handoff [DUB-144](/DUB/issues/DUB-144).
- Heartbeat (assigned [DUB-144](/DUB/issues/DUB-144)): Re-confirmed `npx supabase projects list` empty, `.env` `VITE_SUPABASE_*` empty, Docker unavailable; posted infra blocker + deploy runbook on DUB-144; aligned [DUB-190](/DUB/issues/DUB-190) (blocked) and flagged DUB-PLACEHOLDER → DUB-144 for PM.
- [DUB-204](/DUB/issues/DUB-204): Traced lock reattachment to Paperclip `enqueueWakeup` legacy run query (missing `agentId` guard) + `issues.checkout` missing actor-run validation. Implemented fix + Vitest regressions in upstream `paperclipai/paperclip` `server/`; shipped `contrib/paperclip-DUB-204-full.patch` in Dubiland repo and closed DUB-204 with evidence (merge path: PR to Paperclip, then bump `@paperclipai/server` / `npx paperclipai`).
- [DUB-219](/DUB/issues/DUB-219): Hotfixed active npx tree `~/.npm/_npx/43414d9b790239bb/node_modules/@paperclipai/server/dist/services/{heartbeat,issues}.js` to match DUB-204, SIGTERM’d prior listener, relaunched `npx paperclipai run` (log `/tmp/paperclip-run.log`). Commented [DUB-202](/DUB/issues/DUB-202) + [DUB-203](/DUB/issues/DUB-203); clean DUB-103 snapshot remains with Ops.
- [DUB-222](/DUB/issues/DUB-222): Normalized [DUB-10](/DUB/issues/DUB-10) stale `checkoutRunId` (`6fdbb773-c47b-497b-9747-1946939f1d30`) by `in_progress` → `todo` → `in_progress` (assignee [CMO](/DUB/agents/cmo) unchanged); evidence on [DUB-221](/DUB/issues/DUB-221) + [DUB-207](/DUB/issues/DUB-207); closed DUB-222.
- [DUB-234](/DUB/issues/DUB-234): Hard guards for lock contamination — clear `executionRunId` when row’s run snapshot `issueId` mismatches; validate `adoptStaleCheckoutRun` actor run company/agent/snapshot vs target issue. Patched npx `@paperclipai/server` dist (`heartbeat.js`, `issues.js`); upstream `contrib/paperclip-DUB-234-hard-guard.patch`; `node --check` OK; issue done with handoff comment (restart `paperclipai run` to load dist).
- [DUB-270](/DUB/issues/DUB-270): Queued-run lock reattachment — defer issue `executionRunId` until `claimQueuedRun` (running); no issue-row lock on new queued wakes / deferred promotion; legacy adoption only persists row lock for **running** runs. Hotfix `heartbeat.js` in npx tree; `contrib/paperclip-DUB-270-heartbeat-hotfix-vs-2026.403.0.patch` (tested `patch --dry-run` vs `2026.403.0` tarball); evidence comment on [DUB-264](/DUB/issues/DUB-264); **restart `paperclipai run`** to load dist.
- [DUB-282](/DUB/issues/DUB-282): Reconciled lock-context guardrails on current `paperclipai/paperclip` `main` (clone verify): `issues.checkout` actor snapshot guard + `adoptStaleCheckoutRun` company/agent/snapshot checks + `enqueueWakeup` stale execution snapshot clear + legacy query `agentId` filter + legacy row lock only when run is **running** + removed deferred-promotion eager `executionRunId` stamp. Vitest `issues-checkout-run-context` + `heartbeat-legacy-execution-lock` passed (embedded Postgres). Shipped `contrib/paperclip-DUB-282-reconciled.patch` for upstream PR / release alignment with prior npx dist hotfixes.
- [DUB-284](/DUB/issues/DUB-284): Manager-assignee guardrail for implementation-lane titles (Paperclip `issueService.create` / `update`): reroute manager-class assignees to default IC (`name` ASC), optional `managerAssigneeOverride` + rationale (Zod + server), `activity_log` action `issue.manager_assignee_guardrail`. Tests `server/src/__tests__/issue-manager-assignee-guardrail.test.ts`; handoff `contrib/paperclip-DUB-284-manager-assignee-guardrail.patch`; rollout comment on [DUB-276](/DUB/issues/DUB-276).
- [DUB-292](/DUB/issues/DUB-292): Auth users → `families` trigger + backfill; React `AuthProvider` (shared session state, clears guest mode on sign-in); anon-only client with explicit `persistSession` / `localStorage`; Edge Function rejects missing JWT via `auth.getUser()`; hosted parents load/create `children` UUID profiles for progress APIs. `yarn typecheck` + `yarn generate-audio` OK.
- [DUB-292](/DUB/issues/DUB-292) (reopen): Hosted project `dubiland` ref `csyyrxgubkiqfujmuzjf` (`eu-central-1`), `supabase link`, `db push` 00001–00010, deployed `submit-game-attempt` (JWT verify on), `yarn supabase:types:linked`, local `.env` + `.env.example`, `supabase/.temp/` gitignored; REST `topics` smoke 200.
- [DUB-292](/DUB/issues/DUB-292) (reopen #2): Google “skip” — hosted `external_google_enabled` false + `site_url`/redirect mismatch; Management API set `site_url` 127.0.0.1:5173 + comma `uri_allow_list`; Login stops Google→guest fallback; `useAuth` explicit `location.assign(data.url)`; onboarding i18n + audio; `.env.example` Google note; mock UI → FED.
- [DUB-305](/DUB/issues/DUB-305): Published Paperclip `2026.403.0` lacked DUB-284 guardrail — patched npx `@paperclipai/shared` validators + `@paperclipai/server` `issues.js` / `routes/issues.js`, added `issue-manager-assignee-guardrail.js`, replaced `updateIssueSchema.extend` with `.and()` for ZodEffects. Restarted `paperclipai run` on :3100; verified reroute, override validation, title backfill, activity log; repo handoff `contrib/paperclip-dub-305/`.
- [DUB-190](/DUB/issues/DUB-190): With linked project `csyyrxgubkiqfujmuzjf`, ran `npx supabase db push` (up to date), deployed `automation-recovery-gateway` (`--no-verify-jwt`), live POST smoke **503** with missing-config JSON until Edge secrets (`AUTOMATION_GATEWAY_SECRET`, `WATCHDOG_AGENT_ID`, public `PAPERCLIP_API_URL`, `PAPERCLIP_AUTOMATION_API_KEY`, `RECOVERY_TARGET_AGENT_IDS`) are set. Posted evidence + CLI template on issue; Paperclip run was bound to cancelled DUB-308 so checkout to DUB-190 failed — noted for Ops/runtime.
- [DUB-314](/DUB/issues/DUB-314): Implemented run-context lock integrity in `contrib/paperclip` (`heartbeat.ts` coalesce guard, `issues.ts` `prepareCheckoutHeartbeatRunForIssue` + adopt ordering), exported `contrib/paperclip-DUB-314-lock-context-integrity.patch`; `process_lost_retry` on [DUB-305](/DUB/issues/DUB-305) left actor run snapshot pointing at DUB-305 so DUB-314 checkout stayed 409 until deploy/fresh run — documented on issue.
- Heartbeat: `PAPERCLIP_TASK_ID` = cancelled [DUB-316](/DUB/issues/DUB-316) (QA probe); inbox picked [DUB-292](/DUB/issues/DUB-292) — verified auth migration/config/edge JWT, closed **done** with evidence comment; `POST …/checkout` returned run-context mismatch (snapshot still DUB-316).
- [DUB-330](/DUB/issues/DUB-330): Handbook architecture + `00012` (`handbooks`, `handbook_pages`, `handbook_media_assets`, `child_handbook_progress`) + shared types; `yarn typecheck` OK; remote `db push` **00011**+**00012** applied (2026-04-10).
- [DUB-333](/DUB/issues/DUB-333): Checked out; scope = CTO sign-off (Architect). Commented BE deliverables + asked reassignment; `PATCH assigneeAgentId` → Architect ignored; set **blocked** for PM to reassign.
- [DUB-333](/DUB/issues/DUB-333) (follow-up): Architect/PM created [DUB-336](/DUB/issues/DUB-336) for PM owner correction; BE posted ack + handoff summary (00012, arch doc, types, typecheck); remains **blocked** until reassigned to Architect.
- Heartbeat: `PAPERCLIP_TASK_ID` → cancelled [DUB-309](/DUB/issues/DUB-309). Remote **00011** + **00012** applied (`npx supabase db push --linked`); `yarn supabase:types:linked` + `yarn typecheck` OK; [DUB-190](/DUB/issues/DUB-190) secrets still default-only; evidence comment on [DUB-333](/DUB/issues/DUB-333).
- [DUB-341](/DUB/issues/DUB-341): Paperclip `issues.release` now clears `executionRunId` + `executionAgentNameKey` + `executionLockedAt` with `checkoutRunId`; test `contrib/paperclip/server/src/__tests__/issues-release-execution-lock.test.ts`; `update` path unchanged (already cleared on non–`in_progress` status / assignee change).
- [DUB-342](/DUB/issues/DUB-342): `issueService.update` clears checkout + execution lock columns using **merged** `nextStatus` (`issueData.status ?? existing.status`), so PATCHes without `status` repair cancelled/done rows with stale locks; regression test in `issues-release-execution-lock.test.ts`; handoff `contrib/paperclip-DUB-342-effective-status-execution-lock.patch`.
- [DUB-350](/DUB/issues/DUB-350): `prepareCheckoutHeartbeatRunForIssue` allows rebinding run snapshot from stale wake issue when bound row is `done`/`cancelled` or `assignee_agent_id !== run.agent_id`; orphan snapshot UUID (no row) stays 409; extended `issues-checkout-snapshot-binding.test.ts` (6 tests); patch `contrib/paperclip-DUB-350-checkout-snapshot-rebind.patch`; parent [DUB-345](/DUB/issues/DUB-345) comment posted.
- Heartbeat: Expanded `00013_seed_interactive_handbook.sql` — idempotent `handbooks` row `gardenOfSurprises` + 12 `handbook_pages` (`narration_key` aligned to `common` i18n paths); header + rollback notes; `resolved_game` uses `SELECT DISTINCT id` to avoid duplicate `game_levels` rows on first insert. Remote: `npx supabase db push --linked` applied `00013`.
- [DUB-364](/DUB/issues/DUB-364): Paperclip cross-agent wake authz — `assertAgentHeartbeatInvokeAuthorization` (`tasks:assign` for non-self agent on `wakeup` + `heartbeat/invoke`); Vitest `agent-wake-authz-routes.test.ts`; handoff `contrib/paperclip-DUB-364-cross-agent-wake-authz.patch` for `@paperclipai/server` deploy; parents [DUB-360](/DUB/issues/DUB-360) / [DUB-359](/DUB/issues/DUB-359).
- [DUB-368](/DUB/issues/DUB-368): RLS QA playbook for CTO lane — `npx supabase db query --linked` confirmed four policies on `child_handbook_progress`; security advisors clean for that table; full PostgREST smoke (two JWTs, upsert + cross-family 403/[]) documented on [DUB-362](/DUB/issues/DUB-362) (`2026-04-10T12:14Z` comment).
- [DUB-370](/DUB/issues/DUB-370): Paperclip `issues.checkout` + `mergeCoalescedContextSnapshot` — prevent issue locks while `heartbeat_runs.context_snapshot` stays unbound (DUB-304); Vitest checkout-snapshot + workspace-session + release-lock; **done** with deploy handoff on parent lane.
- [DUB-371](/DUB/issues/DUB-371): Live npx dist hotfix (DUB-370 JS parity) + `paperclipai run` restart; contamination detector all zeros post-restart; handoff comment on [DUB-315](/DUB/issues/DUB-315).
