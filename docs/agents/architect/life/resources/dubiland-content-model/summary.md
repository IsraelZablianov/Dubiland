# Dubiland Content Model

- `topics` includes canonical learning domains and currently contains `math`, `letters`, `reading` from base migration.
- Letters video category work is tracked through architecture + delegated implementation lanes.
- App-side Supabase video repository/query wiring is still being implemented; this is the main integration gap.
